Этот каталог предназначен для сбора информации по функциям ядра,
которая может пригодится для разработки функций-замен, и обработки их результатов.

Информация структурируется по API(!!!) ядра.
Каждый файл имеет имя <kernel_function_API>.txt.

Файл содержит:
	-сигнатуру API функции
	-краткое описание API функции(или ссылка на это описание)
	-через какие ABI(!) функции ядра эта функция реализуется на различных системах, в каких случаях.
	-если вся API функция реализована inline(через inline функции и/или макросы, без использования символов ядра),
какие параметры надо задать при компиляции ядра, чтобы функциональная часть использовала ABI.

Для реализации KEDR очень важны последние два пункта.

При необходимости, можно создавать другие файлы.