1. LTT осуществл€ет, вообще говор€, общесистемный мониторинг. KEDR следит за отдельным модулем, и помимо мониторинга позвол€ет воздействовать на него.

2. LTT может прив€зыватс€ только к маркерам, которые должны быть учтены в коде, KEDR - к вызовам функций. Ќо так как в €дерном коде почти все "важные" функции используют маркеры, можно сказать, что LTT может прив€зыватс€ к функци€м.

3. LTT требует пересборки €дра, причем только с kernel.org. KEDR практически не требует модификации €дра.

4. ” обоих low impact, когда не запущены, когда запущены - не сильно вли€ют на производительность.

5. »меет смысл интеграци€, при которой мы даем LTT возможность выполн€ть call monitoring над нашим модулем. ѕри этом нам самим никто не мешает проводить свой call monitoring. Ёта интеграци€ уже сейчас легко осуществима за счет возможности написани€ probe модул€ дл€ LTT, который перехватывает вывод трассы из payload'ов.  од этого модул€ можно генерировать на основе data-файлов дл€ payload'ов.

5. ѕример probe модул€ - kedr-trace.c