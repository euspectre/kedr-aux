«десь будут примеры индикаторов, которые могут оказатс€ полезными дл€ достижени€ определенных эффектов от fault simulation.

ѕо возможности, это будут data-файлы индикаторов, по которым можно сгенерировать модуль индикатора по шаблону в KEDR.
≈сли одних шаблонов недостаточно, будет приводитс€ модуль индикатора полностью.

≈сли индикатор требует нестандартных fault simulation payloads,
также будет приведен data-файл fault simulation payload'а, на работу с которым рассчитан индикатор.

ƒл€ каждого примера должен быть указан номер ревизии шаблонов, на которых этот пример провер€лс€
(в будущем шаблоны могут мен€тс€, в т.ч. дл€ изменени€ функциональности самих индикаторов).