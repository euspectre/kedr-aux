Here is the place for the tests checking several components each ("global"
tests).

Note that the line between "global" and "local" tests is often not very 
clear, the distinction is made just for convenience.
